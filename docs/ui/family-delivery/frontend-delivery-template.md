# 家属端前端交付模板

适用范围：家属端 Flutter 页面、路由、Tab、卡片区块、消息页、账单页、探视页和 AI 辅助入口的新增或修改。

## 范围

- 变更名称：
- 页面或路由入口：
- 影响用户：家属端用户
- 发布阶段：原型、内测、灰度、正式

## 用户影响

- 用户会看到的变化：
- 不应变化的路径：
- 是否可通过入口隐藏或只读降级：

## 数据或 Mock 来源

- 使用的服务：MockFamilyService、Repository、BFF 或 API
- 关联模型：
- 与老人绑定、账单、探视、消息的边界：

## UI 状态

- 加载态：
- 空态：
- 错误态：
- 弱网或接口失败时的降级：
- 手机端布局影响：

## 页面状态稳定性

- 是否要求 Tab 切换后保持状态：
- 是否要求返回后保持滚动位置：
- 是否存在表单或筛选状态需要保留：

## 可观测性与健康信号

- 健康信号：页面可进入、主信息可见、关键按钮可点击
- 失败信号：白屏、路由错误、状态丢失、关键卡片为空且无说明
- 可保留的日志、断言、稳定 Widget 结构：

## 验证门禁

- 必跑：flutter analyze
- 必跑：flutter test
- 建议手工验证：主 Tab 进入、次级页面返回、异常兜底、状态保持

## 回滚

- 是否可以隐藏入口：
- 是否可以退回只读摘要：
- 若真实接口不稳定，是否退回 Mock：

## 风险与未验证项

- 已知风险：
- 未验证项：