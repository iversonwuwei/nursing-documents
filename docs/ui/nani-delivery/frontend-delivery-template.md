# 护工端前端交付模板

适用范围：护工端 Flutter 页面、任务流、报警处理、登录鉴权、交接班、排班、AI 助手入口的新增或修改。

## 范围

- 变更名称：
- 页面或路由入口：
- 影响用户：护工执行端用户
- 发布阶段：原型、内测、灰度、正式

## 用户影响

- 用户会看到的变化：
- 不应变化的流程：
- 是否可通过入口隐藏、禁用或降级：

## 数据或 Mock 来源

- 使用的服务：MockNaniService、Repository、BFF 或 API
- 关联模型：
- 与任务、报警、交接班、登录态的边界：

## UI 状态

- 加载态：
- 空态：
- 错误态：
- 弱网或接口失败时的降级：
- 手机端布局影响：

## 执行流稳定性

- 是否要求任务状态跨路由保持：
- 是否要求登录态或筛选态持久：
- 是否存在返回后必须恢复的上下文：

## 可观测性与健康信号

- 健康信号：页面可进入、关键任务可操作、异常提示可见、回退路径明确
- 失败信号：鉴权错误、任务状态丢失、报警确认失败、页面白屏
- 可保留的日志、断言、稳定 Widget 结构：

## 验证门禁

- 必跑：flutter analyze
- 必跑：flutter test
- 建议手工验证：登录、任务进入、报警确认、返回和重入路径

## 回滚

- 是否可以隐藏入口：
- 是否可以回退到只读或 Mock 流程：
- 若真实接口不稳定，是否退回本地原型：

## 风险与未验证项

- 已知风险：
- 未验证项：